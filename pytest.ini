[pytest]
pythonpath = .

# Exclude scratch/ — those are one-off/ad-hoc scripts, not unit tests
norecursedirs = scratch .git __pycache__ .venv venv node_modules

# Suppress known upstream deprecation warnings
filterwarnings =
    ignore::sqlalchemy.exc.LegacyAPIWarning
    ignore::DeprecationWarning
